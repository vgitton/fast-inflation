#pragma once

// For size_t
#include <cstddef>
// For int64_t etc
#include <cstdint>

/*! \file */

/*! \ingroup util
    \brief Signed integers.

    These would potentially be replaced by arbitrary precision numbers.
 */
typedef int64_t Num;

/*! \ingroup util
    \brief Use for anything meant to be used as an index. */
typedef uint64_t Index;
